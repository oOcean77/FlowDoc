from __future__ import annotations


def predict_image_only(image_path: str, question: str) -> dict[str, str]:
    return {"answer": "", "status": "unsupported", "reason": "Week-1 placeholder; no VLM is called."}
